# Railway 운영 배포 검증 기록

검증일: 2026-08-31 (Asia/Seoul)

## 배포 및 복구 지점

- Git 브랜치: `employeeTracker`
- PDF 에이전트 작업 전 태그: `employee-tracker-before-pdf-agent-20260831` (`d44789f`)
- Railway 작업 전 태그: `employee-tracker-before-railway-20260831` (`8052021`)
- 운영 Railway 프로젝트/환경/서비스: `screen-monitor` / `production` / `api`
- 운영 배포 ID와 이미지 digest는 Railway 운영 콘솔에서 확인하며 공개 저장소 문서에는 기록하지 않는다.
- 운영 API 주소도 공개 저장소 문서에는 기록하지 않는다.

## 서버 검증 결과

- 배포 상태 `SUCCESS`, `/health` HTTP 200 확인
- 시작 로그에서 보류 중 데이터베이스 마이그레이션 적용 확인
- 백엔드 테스트 59건 통과, API 빌드 통과, EF 모델 미반영 변경 없음
- Railway Bucket 연결 변수와 PostgreSQL/JWT 변수가 설정되어 있음을 값 노출 없이 확인
- 운영 장치 정책 조회 성공
- 자동 보존 정리는 운영 정책에서 `RetentionEnabled=false`이므로 비활성 상태
- 보안 이벤트 등록 API HTTP 200 확인

## 장치 정책 및 백업 상태

- 운영 장치 식별자는 공개 저장소 문서에 기록하지 않는다.
- 모니터링, 백업, 파일 변경, USB, 네트워크, 복원, 자원 제한 정책 활성 확인
- 활성 인벤토리 식별자는 공개 저장소 문서에 기록하지 않는다.
- 인벤토리 상태: `Scanning`
- 백업 업로드는 인벤토리 완료 후 관리자가 백업을 시작해 `BackingUp` 상태가 되어야 진행된다.
- 로컬 암호화 백업 작업 1,158건을 삭제하거나 임의 승인하지 않고 보존했다.

## 이벤트 재전송 안정화

- 대량 로컬 이벤트를 무제한 재전송하던 동작을 먼저 주기당 100건으로 제한했다.
- 100건의 순간 전송도 Railway 로그 초당 제한을 유발해 최종적으로 30초당 10건으로 축소했다.
- 기본 제한값을 직접 검증하는 TDD를 추가했고 Rust 라이브러리 테스트 74건이 통과했다.
- 최종 앱 재실행 후 약 1분 운영 로그에서 보안 이벤트 완료 HTTP 200 10건, 속도 제한 경고 0건을 확인했다.
- Windows 서비스는 `Running`, 데스크톱 앱은 백그라운드 실행 상태다.

## 최종 Windows 설치본

- 파일: `출퇴근 관리 프로그램_2.0.0_x64-setup.exe`
- 크기: 4,076,280 bytes
- 설치본 SHA-256: `495466DED0FB162482F488183240A69C65C293E75F8760BCBF81F7B82AA4EC02`
- 설치된 앱 SHA-256: `38AF4465878F1423D419CC4985D24354A572533DF91998557BEEE66153722304`
- 회사 코드 서명 인증서는 아직 적용되지 않았다.

## 남은 실사용 검증

- 장시간 실행, 네트워크 단절/복구, Windows 재부팅 검증
- 실제 USB 장치 연결과 파일 쓰기 증거 검증
- 전체 인벤토리 완료 시간과 진행률 검증
- 대용량 백업 시작은 저장 비용과 트래픽이 발생하므로 관리자 확인 후 수행
- 배포 설정 파일은 2026-12-01 이전 Railway Infrastructure as Code 형식으로 이전
